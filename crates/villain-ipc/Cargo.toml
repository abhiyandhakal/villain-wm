[package]
name = "villain-ipc"
version = "0.1.0"
edition = "2024"
description = "Smithay-independent IPC types and client for Villain"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
